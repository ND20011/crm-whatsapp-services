// Archivo de configuración de entorno para CRM Condorito
// Este archivo se genera automáticamente durante el build
(function(window) {
  window['env'] = window['env'] || {};
  
  // Configuración para desarrollo (por defecto)
  window['env']['apiUrl'] = 'http://localhost:3000';
  
  // En producción, este archivo será reemplazado con la configuración correcta
})(this);
